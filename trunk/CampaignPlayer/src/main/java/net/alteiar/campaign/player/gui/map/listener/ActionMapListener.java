package net.alteiar.campaign.player.gui.map.listener;


public class ActionMapListener extends MapAdapter {
	protected final GlobalMapListener mapListener;

	public ActionMapListener(GlobalMapListener mapListener) {
		this.mapListener = mapListener;
	}
}
